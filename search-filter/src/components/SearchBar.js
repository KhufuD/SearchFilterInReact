import React from "react";

const SearchBar = () => {
    return (
        <form>
            <label>Filter here</label>
            <input></input>
        </form>
    );
};

export default SearchBar;